#include<bits/stdc++.h>
using namespace std;

int main()
{
    set <int>s;
    s.insert(5);
    s.insert(3);
    s.insert(10);
    s.insert(0);

    cout<<"number of element in set is : "<<s.size()<<endl;
    return 0;
}